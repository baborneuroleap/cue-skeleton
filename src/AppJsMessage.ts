export default class AppJsMessage {
    static get(key: string, args?: string | string[]): string {
        // const mixin = (window as any)?.$mixin;
        // if (mixin?.methods?.getJsMessage) {
        //     return mixin?.methods?.getJsMessage(key, args);
        // }
        // return "";
        return "some";
    }
}